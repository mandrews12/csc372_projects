<div class="item" data-description="{{description}}" data-stock="{{stock}}">
    <img src="{{image}}" alt="{{name}}" height="150">
    <h3>{{name}}</h3>
    <p>${{price}}</p>
    <button class="view-details">View Details</button>
</div>